# Clipboard

When generating content intended to be copied elsewhere — emails, Slack messages, writeups, etc. — automatically copy it to the clipboard with `pbcopy` instead of just displaying it. Still show the content inline so I can review it, but don't wait for me to ask for it to be copied.
